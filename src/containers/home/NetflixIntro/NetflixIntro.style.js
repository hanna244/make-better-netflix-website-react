import styled from 'styled-components'
import { Photo } from '../../../components/Photo/Photo.style'

export const MainImg = styled(Photo)`
  margin-top: 1rem;

  @media (min-width: 60em) {
    margin-top: 0;
  }
`
