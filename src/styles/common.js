import { css } from 'styled-components'

const resetList = css`
  list-style: none;
  padding-left: 0;
  margin-left: 0;
  margin-right: 0;
`

const resetButton = css`
  border: 0;
`

const resetAddress = css`
  font-style: normal;
`

export { resetList, resetButton, resetAddress }
