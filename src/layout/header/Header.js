import React, { Fragment } from 'react'
import { Button, Logo } from '../../components'

const Header = () => {
  return (
    <Fragment>
      <Logo />
      <Button />
    </Fragment>
  )
}

export default Header
