import styled from 'styled-components'
import { ButtonStyle } from '../../components'
import { resetLink } from '../../styles/common'

export const headerLinkStyle = styled.a`
  ${resetLink}
`
export const headerLoginLinkStyle = styled.a`
  ${resetLink}
`
